# BatteryStations
MicroService with in memory DB. Using hibernate as in memory example.

This is an example project built to showcase the functionality of a simple spring boot service with hibernate as DB. 
